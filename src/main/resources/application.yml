

spring:
  redis:
    host: 10.10.11.11
    port: 6379
    lettuce:
      pool:
        max-active: 25
        max-idle: 25
  cache:
    redis:
      time-to-live: 3600s
      blackdata:
        ttl: 5s
      use-key-prefix: false #Ĭ����true������key��ǰ�����cache-name::
      cache-null-values: false  #Ĭ����true������key��ǰ�����cache-name::
    cache-names:
      - blackdata